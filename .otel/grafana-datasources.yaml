apiVersion: 1
datasources:
  - name: Tempo
    type: tempo
    access: proxy
    url: http://tempo:3200
    isDefault: true
    jsonData:
      nodeGraph:
        enabled: true
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus-gateway:9091
