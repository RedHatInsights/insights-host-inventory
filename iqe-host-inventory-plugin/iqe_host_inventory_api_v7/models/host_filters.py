"""
Insights Host Inventory REST Interface

REST interface for the Insights Platform Host Inventory application.

The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from __future__ import annotations

import json
import pprint
import re  # ruff: ignore[unused-import]
from datetime import datetime
from typing import Any
from typing import ClassVar
from typing import Self

from pydantic import BaseModel
from pydantic import ConfigDict
from pydantic import Field
from pydantic import StrictStr
from pydantic import field_validator


class HostFilters(BaseModel):
    """
    Host-level query filters that correspond to the /hosts endpoint query parameters. Stored with the view so the frontend can reconstruct the full query when loading hosts for this view.
    """

    hostname_or_id: StrictStr | None = Field(
        default=None, description="Display name or ID substring search."
    )
    staleness: list[StrictStr] | None = Field(
        default=None, description="Culling states to include."
    )
    registered_with: list[StrictStr] | None = Field(
        default=None, description="Filter by reporting source."
    )
    tags: list[StrictStr] | None = Field(
        default=None, description="Tag filters in namespace/key=value format."
    )
    workspace_name: list[StrictStr] | None = Field(
        default=None, description="Filter by workspace name."
    )
    last_check_in_start: datetime | None = Field(
        default=None, description="Start of last check-in date range (ISO 8601)."
    )
    last_check_in_end: datetime | None = Field(
        default=None, description="End of last check-in date range (ISO 8601)."
    )
    updated_start: datetime | None = Field(
        default=None, description="Start of last-modified date range (ISO 8601)."
    )
    updated_end: datetime | None = Field(
        default=None, description="End of last-modified date range (ISO 8601)."
    )
    system_type: list[StrictStr] | None = Field(default=None, description="Filter by system type.")
    __properties: ClassVar[list[str]] = [
        "hostname_or_id",
        "staleness",
        "registered_with",
        "tags",
        "workspace_name",
        "last_check_in_start",
        "last_check_in_end",
        "updated_start",
        "updated_end",
        "system_type",
    ]

    @field_validator("staleness")
    def staleness_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in {"fresh", "stale", "stale_warning", "unknown"}:
                raise ValueError(
                    "each list item must be one of ('fresh', 'stale', 'stale_warning', 'unknown')"
                )
        return value

    @field_validator("system_type")
    def system_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in {"conventional", "bootc", "edge", "cluster"}:
                raise ValueError(
                    "each list item must be one of ('conventional', 'bootc', 'edge', 'cluster')"
                )
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self | None:
        """Create an instance of HostFilters from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: set[str] = set()

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: dict[str, Any] | None) -> Self | None:
        """Create an instance of HostFilters from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        # raise errors for additional fields in the input
        for _key in obj.keys():
            if _key not in cls.__properties:
                raise ValueError(
                    "Error due to additional fields (not defined in HostFilters) in the input: "
                    + _key
                )

        _obj = cls.model_validate({
            "hostname_or_id": obj.get("hostname_or_id"),
            "staleness": obj.get("staleness"),
            "registered_with": obj.get("registered_with"),
            "tags": obj.get("tags"),
            "workspace_name": obj.get("workspace_name"),
            "last_check_in_start": obj.get("last_check_in_start"),
            "last_check_in_end": obj.get("last_check_in_end"),
            "updated_start": obj.get("updated_start"),
            "updated_end": obj.get("updated_end"),
            "system_type": obj.get("system_type"),
        })
        return _obj
