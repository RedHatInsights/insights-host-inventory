"""
Insights Host Inventory REST Interface

REST interface for the Insights Platform Host Inventory application.

The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from __future__ import annotations

import json
import pprint
import re  # ruff: ignore[unused-import]
from typing import Any
from typing import ClassVar
from typing import Self

from pydantic import BaseModel
from pydantic import ConfigDict
from pydantic import Field
from pydantic import StrictBool


class HostViewCombinedFields(BaseModel):
    """
    Combined sparse fieldset supporting both system_profile and application data fields. The `system_profile` key selects system profile fields, while all other keys select application data fields. The `app_data` key is an explicit shorthand to request all app data. When omitted, all application data is returned by default but no system_profile data is included.
    """

    system_profile: dict[str, StrictBool] | None = Field(
        default=None,
        description="Map of field names to include. Keys are field names, values are always true.",
    )
    app_data: dict[str, StrictBool] | None = Field(
        default=None,
        description="Map of field names to include. Keys are field names, values are always true.",
    )
    advisor: dict[str, StrictBool] | None = Field(
        default=None,
        description="Map of field names to include. Keys are field names, values are always true.",
    )
    vulnerability: dict[str, StrictBool] | None = Field(
        default=None,
        description="Map of field names to include. Keys are field names, values are always true.",
    )
    patch: dict[str, StrictBool] | None = Field(
        default=None,
        description="Map of field names to include. Keys are field names, values are always true.",
    )
    remediations: dict[str, StrictBool] | None = Field(
        default=None,
        description="Map of field names to include. Keys are field names, values are always true.",
    )
    compliance: dict[str, StrictBool] | None = Field(
        default=None,
        description="Map of field names to include. Keys are field names, values are always true.",
    )
    malware: dict[str, StrictBool] | None = Field(
        default=None,
        description="Map of field names to include. Keys are field names, values are always true.",
    )
    __properties: ClassVar[list[str]] = [
        "system_profile",
        "app_data",
        "advisor",
        "vulnerability",
        "patch",
        "remediations",
        "compliance",
        "malware",
    ]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self | None:
        """Create an instance of HostViewCombinedFields from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: set[str] = set()

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: dict[str, Any] | None) -> Self | None:
        """Create an instance of HostViewCombinedFields from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        # raise errors for additional fields in the input
        for _key in obj.keys():
            if _key not in cls.__properties:
                raise ValueError(
                    "Error due to additional fields (not defined in HostViewCombinedFields) in the input: "
                    + _key
                )

        _obj = cls.model_validate({
            "system_profile": obj.get("system_profile"),
            "app_data": obj.get("app_data"),
            "advisor": obj.get("advisor"),
            "vulnerability": obj.get("vulnerability"),
            "patch": obj.get("patch"),
            "remediations": obj.get("remediations"),
            "compliance": obj.get("compliance"),
            "malware": obj.get("malware"),
        })
        return _obj
