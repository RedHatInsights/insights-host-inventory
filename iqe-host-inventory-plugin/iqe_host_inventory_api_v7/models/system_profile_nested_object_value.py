"""
Insights Host Inventory REST Interface

REST interface for the Insights Platform Host Inventory application.

The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from __future__ import annotations

import json
import pprint
from typing import Any
from typing import Self

from pydantic import BaseModel
from pydantic import ConfigDict
from pydantic import Field
from pydantic import ValidationError
from pydantic import field_validator

SYSTEMPROFILENESTEDOBJECTVALUE_ONE_OF_SCHEMAS = ["Dict[str, SystemProfileNestedObjectValue]"]


class SystemProfileNestedObjectValue(BaseModel):
    """
    SystemProfileNestedObjectValue
    """

    # data type: Dict[str, SystemProfileNestedObjectValue]
    oneof_schema_1_validator: dict[str, SystemProfileNestedObjectValue | None] | None = Field(
        default=None, description="An arbitrary object that does not allow empty string keys."
    )
    actual_instance: dict[str, SystemProfileNestedObjectValue] | None = None
    one_of_schemas: set[str] = {"Dict[str, SystemProfileNestedObjectValue]"}

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError(
                    "If a position argument is used, only 1 is allowed to set `actual_instance`"
                )
            if kwargs:
                raise ValueError(
                    "If a position argument is used, keyword arguments cannot be used."
                )
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator("actual_instance")
    def actual_instance_must_validate_oneof(cls, v):
        if v is None:
            return v

        instance = SystemProfileNestedObjectValue.model_construct()
        error_messages = []
        match = 0
        # validate data type: Dict[str, SystemProfileNestedObjectValue]
        try:
            instance.oneof_schema_1_validator = v
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        if match > 1:
            # more than 1 match
            raise ValueError(
                "Multiple matches found when setting `actual_instance` in SystemProfileNestedObjectValue with oneOf schemas: Dict[str, SystemProfileNestedObjectValue]. Details: "
                + ", ".join(error_messages)
            )
        elif match == 0:
            # no match
            raise ValueError(
                "No match found when setting `actual_instance` in SystemProfileNestedObjectValue with oneOf schemas: Dict[str, SystemProfileNestedObjectValue]. Details: "
                + ", ".join(error_messages)
            )
        else:
            return v

    @classmethod
    def from_dict(cls, obj: str | dict[str, Any]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str | None) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        if json_str is None:
            return instance

        error_messages = []
        match = 0

        # deserialize data into Dict[str, SystemProfileNestedObjectValue]
        try:
            # validation
            instance.oneof_schema_1_validator = json.loads(json_str)
            # assign value to actual_instance
            instance.actual_instance = instance.oneof_schema_1_validator
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError(
                "Multiple matches found when deserializing the JSON string into SystemProfileNestedObjectValue with oneOf schemas: Dict[str, SystemProfileNestedObjectValue]. Details: "
                + ", ".join(error_messages)
            )
        elif match == 0:
            # no match
            raise ValueError(
                "No match found when deserializing the JSON string into SystemProfileNestedObjectValue with oneOf schemas: Dict[str, SystemProfileNestedObjectValue]. Details: "
                + ", ".join(error_messages)
            )
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> dict[str, Any] | dict[str, SystemProfileNestedObjectValue] | None:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


# TODO: Rewrite to not use raise_errors
SystemProfileNestedObjectValue.model_rebuild(raise_errors=False)
