"""
Insights Host Inventory REST Interface

REST interface for the Insights Platform Host Inventory application.

The version of the OpenAPI document: 1.0.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from __future__ import annotations

import json
import pprint
import re  # ruff: ignore[unused-import]
from typing import TYPE_CHECKING
from typing import Any
from typing import Self

from pydantic import BaseModel
from pydantic import Field
from pydantic import ValidationError
from pydantic import field_validator

from iqe_host_inventory_api_v7.models.host_view_filter_comparison import HostViewFilterComparison
from iqe_host_inventory_api_v7.models.system_profile_nested_object_value import (
    SystemProfileNestedObjectValue,
)

HOSTVIEWCOMBINEDFILTERVALUE_ANY_OF_SCHEMAS = [
    "Dict[str, HostViewFilterComparison]",
    "Dict[str, SystemProfileNestedObjectValue]",
]


class HostViewCombinedFilterValue(BaseModel):
    """
    HostViewCombinedFilterValue
    """

    # data type: Dict[str, SystemProfileNestedObjectValue]
    anyof_schema_1_validator: dict[str, SystemProfileNestedObjectValue | None] | None = Field(
        default=None, description="An arbitrary object that does not allow empty string keys."
    )
    # data type: Dict[str, HostViewFilterComparison]
    anyof_schema_2_validator: dict[str, HostViewFilterComparison] | None = None
    if TYPE_CHECKING:
        actual_instance: (
            dict[str, HostViewFilterComparison] | dict[str, SystemProfileNestedObjectValue] | None
        ) = None
    else:
        actual_instance: Any = None
    any_of_schemas: set[str] = {
        "Dict[str, HostViewFilterComparison]",
        "Dict[str, SystemProfileNestedObjectValue]",
    }

    model_config = {
        "validate_assignment": True,
        "protected_namespaces": (),
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError(
                    "If a position argument is used, only 1 is allowed to set `actual_instance`"
                )
            if kwargs:
                raise ValueError(
                    "If a position argument is used, keyword arguments cannot be used."
                )
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator("actual_instance")
    def actual_instance_must_validate_anyof(cls, v):
        instance = HostViewCombinedFilterValue.model_construct()
        error_messages = []
        # validate data type: Dict[str, SystemProfileNestedObjectValue]
        try:
            instance.anyof_schema_1_validator = v
            return v
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # validate data type: Dict[str, HostViewFilterComparison]
        try:
            instance.anyof_schema_2_validator = v
            return v
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        if error_messages:
            # no match
            raise ValueError(
                "No match found when setting the actual_instance in HostViewCombinedFilterValue with anyOf schemas: Dict[str, HostViewFilterComparison], Dict[str, SystemProfileNestedObjectValue]. Details: "
                + ", ".join(error_messages)
            )
        else:
            return v

    @classmethod
    def from_dict(cls, obj: dict[str, Any]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        # deserialize data into Dict[str, SystemProfileNestedObjectValue]
        try:
            # validation
            instance.anyof_schema_1_validator = json.loads(json_str)
            # assign value to actual_instance
            instance.actual_instance = instance.anyof_schema_1_validator
            return instance
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into Dict[str, HostViewFilterComparison]
        try:
            # validation
            instance.anyof_schema_2_validator = json.loads(json_str)
            # assign value to actual_instance
            instance.actual_instance = instance.anyof_schema_2_validator
            return instance
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if error_messages:
            # no match
            raise ValueError(
                "No match found when deserializing the JSON string into HostViewCombinedFilterValue with anyOf schemas: Dict[str, HostViewFilterComparison], Dict[str, SystemProfileNestedObjectValue]. Details: "
                + ", ".join(error_messages)
            )
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(
        self,
    ) -> (
        dict[str, Any]
        | dict[str, HostViewFilterComparison]
        | dict[str, SystemProfileNestedObjectValue]
        | None
    ):
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())
